    UserServiceImpl:

    override fun getByLogin(login: String): User {
        val user = userRepo.findByLogin(login)?: throw NullPointerException()
        return (user ?: null) as User
    }

    исключение!!!!!!!

    исключения в save: Handmadetype, UserCridentials